import { ResolverPathOptions } from './index-server.js';

interface LoaderOptions {
    resolverPath: ResolverPathOptions;
    includeRPCFolders?: string[];
}
interface Loader {
    _compiler?: {
        name: string;
        context: string;
    };
    resource: string;
    cacheable: (enabled: boolean) => void;
    query: LoaderOptions;
}

export { Loader as L, LoaderOptions as a };
