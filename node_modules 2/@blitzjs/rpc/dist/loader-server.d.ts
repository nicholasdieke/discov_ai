import { L as Loader, a as LoaderOptions } from './loader-utils-65deaa51.js';
import './index-server.js';
import 'blitz';
import 'next';
import './index-browser.js';
import '@tanstack/react-query';
import '@blitzjs/auth';
import 'zod';

declare function loader(this: Loader, input: string): Promise<string>;
declare function transformBlitzRpcServer(src: string, id: string, root: string, resolvers: string[], options?: LoaderOptions): Promise<string>;
declare function collectResolvers(directory: string, rpcFolders: string[], pageExtensions: string[]): Promise<string[]>;
declare function recursiveFindResolvers(dir: string, filter: RegExp, ignore?: RegExp, arr?: string[], rootDir?: string, rpcFolders?: string[]): Promise<string[]>;

export { collectResolvers, loader, recursiveFindResolvers, transformBlitzRpcServer };
